 When running Spring Boot, if we make changes to our source code, then we need to restart the application manually to see the changes.

However, we can use a tool something like springboot-dev-tools

